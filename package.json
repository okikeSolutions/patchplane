{
  "name": "patchplane",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:client": "bun run --cwd apps/client dev",
    "dev:backend": "bun run --cwd packages/backend dev",
    "build:client": "bun run --cwd apps/client build",
    "typecheck": "bun run --cwd packages/domain typecheck && bun run --cwd packages/backend typecheck && bun run --cwd apps/client typecheck",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "fmt": "oxfmt .",
    "fmt:check": "oxfmt --check .",
    "format": "oxfmt .",
    "format:check": "oxfmt --check ."
  },
  "dependencies": {
    "effect": "latest"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^25.5.0",
    "oxfmt": "latest",
    "oxlint": "latest",
    "typescript": "^5.9.3"
  },
  "packageManager": "bun@1.3.10"
}
